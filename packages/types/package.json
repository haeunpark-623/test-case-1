{
  "name": "@conduit/types",
  "version": "0.1.0",
  "private": true,
  "description": "Conduit shared types — M-SHARED-TYPES",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:unit": "vitest run --coverage",
    "test:int": "echo 'no integration tests for types package'"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.0.5",
    "typescript": "^5.5.3",
    "vitest": "^2.0.5"
  }
}
