# Frontend dev profile — workspace-scoped (.env.dev), LOCAL.md §1.5.1 (e) 채택
# Vite는 cwd의 .env.<mode>를 자동 로드. VITE_* 키만 client에 노출.

VITE_API_BASE_URL=http://localhost:4000/api
VITE_APP_TITLE=Conduit (dev)
